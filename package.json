{
  "name": "subschema-service",
  "version": "1.0.0",
  "description": "contains schema that gets stitched into gateway service",
  "main": "index.js",
  "scripts": {
    "build": "tsc --build",
    "dev": "tsnd --no-notify --respawn --transpile-only -- src/index",
    "start": "npm run build && node dist/index",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@graphql-tools/schema": "^8.3.0",
    "express": "^4.17.1",
    "express-graphql": "^0.12.0",
    "graphql": "^15.6.1",
    "ts-node-dev": "^1.1.8"
  },
  "devDependencies": {
    "@types/express": "^4.17.13",
    "typescript": "^4.4.4"
  }
}
